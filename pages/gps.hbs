---
nav: gps
permalink: "<%= configs[nav].types.map(function(i){ return '/'+nav+'/' + (i ? i + '/' : ''); }).join('\\n') %>"
type: "<%= permalink.replace(new RegExp('^\/'+nav+'\/|\/$', 'g'), '') %>"
counter: [ 0 ]
title: "<%= ( name = configs[nav].TYPES[configs[nav].types.indexOf(permalink.replace(new RegExp('^\/'+nav+'\/|\/$', 'g'), ''))] ) ? name + '-' : ''  %>产品展示"
keywords: 合肥GPS
description: 捷通数码专注于GPS监控产品发展，研发新型高品质GPS产品，系统稳定性更高，为广东、合肥、广州、佛山和顺德等地提供专业的GPS产品。
---
<div class="main">
<div class="maincontainer png fixed_width pos_center">
  <div class="twocol">
  <div class="twocol_l">
{{>about}}
  </div>
  <div class="twocol_r">
    <div class="context png">
      {{>bread}}
      <div class="clear"></div>
      <div class="prods">
        {{#eachProperty gps}}
        {{#contains value.data.type ../type}}
        <div class="prod">
          <a href="{{value.filename}}"><img src="{{value.data.image}}" width="130" height="100" /></a>
          <a href="{{value.filename}}">{{value.data.title}}</a><br />
          {{value.data.description}}
        </div>
        {{/contains}}
        {{/eachProperty}}
      </div>
    </div>
  </div>
  <div class="clear"></div>
  </div>
</div>
</div>
